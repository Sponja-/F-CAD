from elements import Constant
import numpy as np

zero = Number(0)
one_half = Number(0.5)
one = Number(1)
two = Number(2)
ten = Number(10)
pi = Number(np.pi)
e = Number(np.e)
infinity = Number(np.inf)
negative_infinity = Number(-np.inf)